const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const invoices = await prisma.invoice.findMany({ include: { project: { include: { client: true, consultant: true } } }, orderBy: { createdAt: 'desc' } }); console.log('INVOICES SUCCESS', invoices.length); } catch (e) { console.error('ERROR INVOICE', e); } try { const vendorInvoices = await prisma.vendorInvoice.findMany({ include: { project: { include: { client: true, consultant: true } }, vendor: true }, orderBy: { createdAt: 'desc' } }); console.log('VENDOR INVOICES SUCCESS', vendorInvoices.length); } catch (e) { console.error('ERROR VENDOR', e); } } main();  
