const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const invoices = await prisma.invoice.findMany({ include: { project: { include: { client: true, consultant: { include: { vendor: true } } } } }, orderBy: { createdAt: 'desc' } }); console.log('INVOICES SUCCESS', invoices.length); } catch (e) { console.error('ERROR INVOICE', e); } } main();  
