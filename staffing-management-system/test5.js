const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const invoices = await prisma.invoice.findMany({ include: { project: { include: { client: true, consultant: { include: { vendor: true } } } } }, orderBy: { createdAt: 'desc' } }); const res = invoices.map(inv = id: inv.id, invoiceNumber: inv.invoiceNumber, projectName: inv.project.name })); console.log('MAPPED SUCCESSFULLY', res.length); } catch (e) { console.error('ERROR MAPPING:', e); } } main();  
